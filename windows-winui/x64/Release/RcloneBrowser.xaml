<Window
    x:Class="GlassPlayer.RcloneBrowser"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GlassPlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Remote Browser">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Connection Bar -->
        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:ConnectionId='6' x:Name="UrlField" PlaceholderText="http://localhost:8080"                            />
            <Button x:ConnectionId='7' x:Name="ConnectBtn" Content="Connect" Grid.Column="1" Margin="12,0,0,0"                         />
        </Grid>

        <!-- Navigation Bar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0,0,0,12">
            <Button x:ConnectionId='3' x:Name="BackBtn" Content="←" Width="36"                       IsEnabled="False"/>
            <Button x:ConnectionId='4' x:Name="UpBtn" Content="↑" Width="36"                     IsEnabled="False"/>
            <Button x:ConnectionId='5' x:Name="RefreshBtn" Content="⟳" Width="36"                          IsEnabled="False"/>
        </StackPanel>

        <!-- Directory/Files ListView -->
        <ListView x:ConnectionId='2' x:Name="DirectoryList" Grid.Row="2"                                   SelectionMode="Single">
            <!-- Items populated dynamically -->
        </ListView>
    </Grid>
</Window>

