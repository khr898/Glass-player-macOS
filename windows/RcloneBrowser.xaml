<Window
    x:Class="GlassPlayer.RcloneBrowser"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GlassPlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Remote Browser">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Connection Bar -->
        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="UrlField" PlaceholderText="http://localhost:8080" KeyDown="OnUrlFieldKeyDown"/>
            <Button x:Name="ConnectBtn" Content="Connect" Grid.Column="1" Margin="12,0,0,0" Click="OnConnectClicked"/>
        </Grid>

        <!-- Navigation Bar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0,0,0,12">
            <Button x:Name="BackBtn" Content="←" Width="36" Click="OnBackClicked" IsEnabled="False"/>
            <Button x:Name="UpBtn" Content="↑" Width="36" Click="OnUpClicked" IsEnabled="False"/>
            <Button x:Name="RefreshBtn" Content="⟳" Width="36" Click="OnRefreshClicked" IsEnabled="False"/>
        </StackPanel>

        <!-- Directory/Files ListView -->
        <ListView x:Name="DirectoryList" Grid.Row="2" DoubleTapped="OnItemDoubleTapped" SelectionMode="Single">
            <!-- Items populated dynamically -->
        </ListView>
    </Grid>
</Window>
